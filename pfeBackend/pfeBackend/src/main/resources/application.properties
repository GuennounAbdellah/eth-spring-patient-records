# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/eth_project
spring.datasource.username=root
spring.datasource.password=MyStr0ngP@ss

# Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.use_sql_comments=true

# JWT Configuration
app.jwtSecret=YourSuperSecretKeyForJWT
app.jwtExpirationMs=86400000 # 24 hours in milliseconds

# Ethereum (Web3j) Configuration
ethereum.node.url=https://goerli.infura.io/v3/YOUR_INFURA_PROJECT_ID
ethereum.contract.address=0xYourContractAddress
ethereum.wallet.private-key=YourPrivateKey
ethereum.gas.price=20000000000 # Gas price in Wei
ethereum.gas.limit=6721975 # Gas limit